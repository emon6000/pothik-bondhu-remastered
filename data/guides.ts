
import { Guide } from '../types';

// No static dummy data. 
// Data comes from the Database via API.
export const guidesByDistrict: Record<string, Guide[]> = {};

export const getAllGuides = () => {
  return [];
};
